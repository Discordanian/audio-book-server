[package]
name = "audio-book-server"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock"] }
percent-encoding = "2"
wasi = "0.14"
